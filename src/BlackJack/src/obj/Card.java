package obj;import java.util.ArrayList;import java.util.Scanner;public class Card {    // 배부할 덱    ArrayList<Integer> Deck = new ArrayList<>();    // 초반    int cnt = 0;    public int getCnt() {        return cnt;    }    public void setCnt(int cnt) {        this.cnt = cnt;    }    // 생성자    public Card() {        setDeck();    }    // 카드 세팅    public ArrayList<Integer> setDeck() {        for (int i = 0; i < 11; i++) {            Deck.add(i + 1);        }        return Deck;    }    public int rollDeck() {        int card = 0;        if (cnt == 0) {            card = Deck.get((int) (Math.random() * 9) + 1);            Deck.remove(Deck.get(card));            cnt += 1;        } else {            card = Deck.get((int) (Math.random() * 10) + 1);            Deck.remove(Deck.get(card));        }        return card;    }}